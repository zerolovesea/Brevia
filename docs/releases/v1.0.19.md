# Brevia v1.0.19

## Improvements

- Make startup feel responsive while worker initialization continues in the background.
- Automatically generate a meeting summary after recording when summary settings are configured.
- Add descriptions and quality/speed ratings to built-in summary models.
- Keep the workspace assignment menu correctly positioned after its parent menu closes.
