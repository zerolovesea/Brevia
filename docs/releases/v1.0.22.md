# Brevia v1.0.22

## Fixes

- After permanently deleting meetings, refresh Recently Deleted from the local database so the displayed list always reflects the actual deletion result.
- Check for updates silently after startup, so a new release is discovered without a manual check.
