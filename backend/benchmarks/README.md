# Benchmark audio

`baseline-zh.wav` is a 14.6-second, 16 kHz mono PCM WAV used by
`python -m backend.bench_refinement`. It is a test-only copy of the Chinese
example recording and is excluded from application packaging.
